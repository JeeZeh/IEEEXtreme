from pprint import pprint

n = int(input())

class Node:
    def __init__(self, id):
        self.id = id
        self.left = self.right = self.left_w = self.right_w = None 

nodes = {1: new Node(1)}

for i in range(n-1):
    n1, n2, w = list(map(int, input().split(" ")))
    node1 = new Node(n1)
    node2 = new Node
    
    tree[p] = {"c": c, "w": w}
    tree[c] = {"c": p, "w": w}

root = new Node(1)

for node in tree:
    root.left = new Node(2)

    
pprint(tree)
